package com.wallet.service;

/**
 * in this service we will add the core transactional system
 * including transfer money between accounts
 * sold check and other related things
 */
public class WalletService {
    // first thing first we need a database connection
    // we have to be careful about this since database connection should be closed as soon as its complete
    // at the same time insuring the transaction has made correctly wih the appropriate isolation level
    // performance and atomicity very important here




}
